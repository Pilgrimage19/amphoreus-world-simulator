# ADR-0002：大模型只作为叙事层

日期：2026-09-04  
状态：接受

## 背景

大模型能改善文本变化度，但其输出不稳定、不可完全复现，并可能添加不存在的事实。项目的随机性应来自属性与规则组合。

## 决定

大模型只读取经过筛选的结构化事件，用于生成对话、信件、年代记和结局摘要。它不能修改模拟状态、选择角色行动或决定胜负。

模板叙事是必须维护的默认后端；本地模型和在线模型均为可选插件。

## 后果

- 无网络、无API费用时仍能完整运行；
- 模拟结果可复现；
- 需要定义严格的事件schema和事实校验；
- 文本可能有表达差异，但不能产生新的世界事实。

